# pg_dump - allows us to make logical backups
## this tool allow us to make selective backups using sql backups
## in that way we can make a backup of a object or objects in the database

### use
pg_dumpall --help
